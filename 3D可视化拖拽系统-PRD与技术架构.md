# 3D 可视化拖拽系统 PRD 与技术架构

版本：V1.1  
日期：2026-08-13  
参考产品：Avue Data（`https://data.avuejs.com/`）的低代码拖拽、组件面板、画布编排和属性配置模式；本产品将二维组件模型扩展为可交互的三维场景。

## 1. 产品概述

### 1.1 产品定位

面向数据可视化工程师、运营人员、售前和行业实施人员的 Web 端 3D 可视化低代码平台。用户从左侧组件库拖拽基础/高级组件到三维画布，通过右侧属性面板、数据面板、时间轴和交互编排完成场景制作，并发布为可嵌入、可分享、可大屏运行的 3D 应用。

### 1.2 核心价值

- 降低 3D 场景制作门槛，常见场景无需编写 Three.js/WebGL 代码。
- 组件、数据、交互、主题和资源资产标准化，支持多人复用。
- 编辑态与运行态解耦，保证复杂场景可预览、可发布、可回滚。
- 通过插件机制接入行业模型、IoT 实时数据和企业内部系统。

### 1.3 目标与边界

**V1 目标**：完成单场景编辑、组件拖拽、属性配置、静态/HTTP/WebSocket 数据绑定、事件联动、模板复用、发布分享和基础权限。  
**暂不纳入 V1**：多人实时协同编辑、移动端编辑、完整 BIM 编辑、复杂物理仿真、离线客户端和专业 DCC 建模能力。

## 2. 用户与典型场景

| 角色 | 目标 | 核心权限 |
|---|---|---|
| 平台管理员 | 管理租户、用户、素材、组件和发布策略 | 全局配置、审计、权限 |
| 可视化设计师 | 搭建并发布高质量 3D 场景 | 创建/编辑/发布项目 |
| 数据工程师 | 配置数据源和转换逻辑 | 数据源、脚本、接口 |
| 业务运营 | 修改标题、指标、告警阈值等受控参数 | 受限编辑、预览 |
| 终端访客 | 查看、筛选、交互和全屏运行 | 只读 |

典型场景：园区/工厂数字孪生、智慧城市、机房监控、能源态势、物流仓储、展厅大屏、设备三维运维。

## 3. 信息架构与页面

1. **工作台**：项目列表、模板、最近编辑、收藏、发布状态。
2. **场景编辑器**：顶部工具栏、左侧资源/组件区、中部 3D 画布、右侧属性区、底部时间轴/事件区。
3. **数据中心**：数据源、接口凭证、字段映射、转换器、实时连接状态。
4. **素材中心**：模型、纹理、字体、HDR、图标、粒子预设、版本和标签。
5. **组件市场**：官方组件、团队组件、第三方插件、评分和版本。
6. **发布中心**：草稿保存、发布版本、访问链接、版本回滚和基础运行错误日志。

## 4. 编辑器功能需求

### 4.1 顶部工具栏

- 项目名称、保存状态、撤销/重做、复制/粘贴、快捷键帮助。
- 预览、调试、发布、版本历史、导入/导出 JSON、全屏画布。
- 视图控制：透视/正交、网格、坐标轴、吸附、线框、阴影、后处理开关。
- 性能面板：FPS、Draw Call、三角面数、纹理内存、场景节点数。

### 4.2 左侧组件库

支持分类、搜索、收藏、最近使用、拖拽预览和组件说明。组件分为：

**基础组件**：空节点、组、立方体、球体、圆柱、平面、文字、图片、图标、按钮、标签、直线、坐标轴、灯光、相机、天空盒、背景、粒子发射器。

**数据组件**：柱状/折线/饼图 3D、仪表盘、热力网格、散点、路径流线、数字翻牌、排名、进度体、告警标记。

**高级组件**：GLTF/OBJ 模型、BIM 构件树、GIS 地图、地形、体积云、等值面、地下管网、设备孪生、轨迹回放、时序数据、传感器标签、区域选中、模型剖切、测量工具。

**系统组件**：交互控制器、后处理、录屏、时间轴、图例、筛选器、弹窗、数据面板、加载器。

每个组件必须声明 `type`、版本、输入属性 schema、数据 schema、事件 schema、资源依赖、编辑器图标和运行时入口。

### 4.3 3D 画布

- 基于 WebGL/WebGPU 的场景视口，支持轨道旋转、平移、缩放、第一人称和漫游模式。
- 鼠标/触控选择、框选、多选、层级树定位、局部坐标/世界坐标切换。
- 三轴 Gizmo：移动、旋转、缩放；数值输入、角度吸附、网格吸附和对齐分布。
- 辅助显示：网格、坐标轴、包围盒、锚点、碰撞体、视锥体、灯光范围、标签连线。
- 大场景能力：节点分块、LOD、视锥剔除、实例化、渐进加载和资源缓存。
- 空场景、加载中、资源失败、WebGL 不可用、低性能降级均有明确状态反馈。

### 4.4 场景树与图层

- 树形层级、拖动改变父子关系、锁定、隐藏、只读、重命名、分组、收藏。
- 节点支持标签和业务 ID；可按名称、类型、业务 ID 搜索。
- 组件与模型的层级变更需校验循环引用、坐标继承和事件作用域。

### 4.5 属性面板

右侧采用 schema 驱动表单，至少包括：

- 变换：位置、旋转、缩放、锚点、父节点、坐标系。
- 外观：材质、颜色、透明度、贴图、粗糙度、金属度、发光、线框、可见性。
- 光照：灯光类型、强度、颜色、阴影、环境光和后处理。
- 文本/UI：内容、字体、字号、朝向、描边、背景、布局和自适应。
- 数据：数据源、字段映射、过滤器、聚合、单位、刷新周期、异常值策略。
- 动画：关键帧、循环、缓动、触发条件和播放速度。
- 交互：点击/悬停/双击/进入视野/数据变化事件及动作列表。
- 高级：渲染层、深度测试、排序、LOD、性能预算、权限和业务扩展字段。

### 4.6 数据与交互编排

- 数据源类型：静态 JSON、REST、GraphQL、WebSocket、MQTT（通过服务端代理）、CSV/Excel 导入。
- 字段映射：组件属性绑定、表达式、格式化、单位换算、颜色分段、阈值规则。
- 转换器：过滤、排序、聚合、窗口计算、坐标转换、脚本转换（沙箱执行）。
- 事件动作：聚焦节点、显示/隐藏、变色、播放动画、打开弹窗、切换场景、请求接口、广播消息。
- 事件总线支持场景级、组级、组件级作用域，提供防抖、节流、错误重试和循环检测。

### 4.7 时间轴与动画

- 场景时间轴、组件时间轴、关键帧、轨道、缓动、循环、速度和片段剪辑。
- 支持实时数据驱动动画、轨迹回放、时间范围筛选和暂停/继续。

### 4.8 预览、发布与分享

- 预览模式隐藏编辑器辅助线，支持断点调试、网络/数据/性能日志。
- 发布前校验：资源完整性、数据源凭证、循环事件、性能预算、未绑定字段和浏览器兼容性。
- 发布产物生成不可变版本号；V1 支持草稿、发布、历史版本和回滚，不做多环境、灰度发布和复杂域名管理。
- 发布后生成一个访问链接，支持只读查看和 iframe 嵌入；V1 使用项目访问权限控制，不单独建设复杂 Token 管理。
- 基础运行错误日志：记录资源加载失败、数据请求失败、WebSocket 断开、脚本异常和 WebGL 初始化失败。

#### 发布中心 MVP 交互

```text
编辑草稿 → 自动保存/手动保存 → 发布前校验 → 生成不可变版本 → 生成访问链接
                                      │
                                      └→ 历史版本列表 → 选择稳定版本回滚
```

- **草稿保存**：编辑器自动保存场景 JSON、资源引用和数据绑定；使用 `revision` 防止覆盖，页面刷新后可恢复。
- **发布版本**：将当前草稿固化为版本快照，生成版本号和发布时间；发布版本不再随草稿修改而变化。
- **访问链接**：每个项目生成一个只读运行地址，访问者只能查看已发布版本；链接失效或项目无发布版本时返回明确提示。
- **版本回滚**：从历史版本中选择一个版本，将其设为当前运行版本；不删除历史版本，回滚操作记录审计日志。
- **基础运行错误日志**：按项目/版本记录错误类型、时间、浏览器、资源或接口标识和错误摘要；V1 只提供列表查询和清理，不建设完整 APM 平台。

## 5. 需求优先级与验收指标

| 优先级 | 能力 | V1 验收 |
|---|---|---|
| P0 | 拖拽、场景树、Gizmo、属性 schema、保存/加载 | 100 个节点场景可编辑，刷新后状态一致 |
| P0 | 静态/REST 数据绑定、事件联动、预览发布 | 端到端链路成功率 ≥99% |
| P0 | GLTF 模型、文字、灯光、相机、基础图表 | 主流 Chrome/Edge 可运行 |
| P1 | WebSocket、时间轴、模板、素材中心、版本回滚 | 1-5 万条实时数据经批量合并后稳定刷新 |
| P1 | 性能分析、LOD、实例化、权限 | 编辑器可查看基础 FPS 和资源指标 |
| P2 | 插件市场、多人协同、WebGPU、BIM/GIS 深度能力 | 作为后续版本专项验收 |

单机 MVP 建议质量指标：编辑器首屏 ≤5 秒（缓存命中）、发布场景首屏 ≤8 秒、自动保存成功率 ≥99%、关键操作 P95 响应 ≤300ms；可用性目标为单机部署月度 ≥99%，不承诺高可用。

## 6. 总体技术架构

### 6.1 分层架构（单台腾讯云 CVM 低成本部署）

```text
浏览器
 ├─ Editor Shell（React + TypeScript）
 ├─ Scene Runtime（Three.js/WebGPU 抽象层）
 ├─ Component SDK（组件注册、Schema、事件、数据适配）
 ├─ State/History（Zustand/Immer + Command History）
 └─ Worker 层（解析、缩略图、数据转换、空间索引）
          │ REST/WebSocket
Nginx（HTTPS、静态文件、反向代理）
          │
FastAPI 模块化单体（REST/WebSocket/BFF）
 ├─ 项目与版本 ├─ 数据源代理 ├─ 素材 ├─ 发布 ├─ 权限 └─ 审计
          │
PostgreSQL（元数据） + Redis（缓存/锁/轻量队列）
          │
本地数据盘 `/data/3dvision`（模型、贴图、发布产物、备份）

全部运行在一台腾讯云 CVM：4 核 CPU、4GB 内存、70GB SSD 系统盘。
```

### 6.2 前端技术选型

- React 18 + TypeScript + Vite；组件 UI 使用 Ant Design 或现有企业设计系统。
- 3D：Three.js（兼容 WebGL/WebGPU），`@react-three/fiber` 仅作为可选封装；复杂场景运行时建议保留原生 Three 场景管理。
- 状态：Zustand + Immer；历史记录采用命令模式，避免直接深拷贝整个场景。
- Schema 表单：JSON Schema + 自研控件注册；属性、数据、事件共用 schema 协议。
- 拖拽：dnd-kit；快捷键：自研 keymap；面板布局：可停靠布局库。
- Worker：GLTF 解析、缩略图、数据转换、空间索引放入 Web Worker；大模型使用 Draco/Meshopt/KTX2。
- 渲染性能：InstancedMesh、LOD、Frustum Culling、纹理压缩、按需渲染和资源引用计数。

### 6.3 后端与基础设施（Python/FastAPI）

- Python 3.11+ + FastAPI + Uvicorn/Gunicorn；按领域拆分模块，V1 使用模块化单体，不引入微服务。
- Pydantic v2 定义请求/响应和场景 schema；SQLAlchemy 2 + Alembic 负责 PostgreSQL 数据访问和迁移。
- WebSocket 由 FastAPI 提供；实时数据先采用单进程内连接管理，后续拆独立网关。
- PostgreSQL 存项目、版本、用户、权限、数据源定义和审计；JSONB 存场景文档与 schema 扩展。
- 模型、贴图、HDR、字体、发布静态文件保存到本机 `/data/3dvision/assets` 和 `/data/3dvision/releases`，数据库只保存路径、hash、大小和 MIME 元数据。
- Redis 用于会话、发布锁、缓存、限流和轻量任务队列；使用 RQ 或 Arq，限制 Worker 并发为 1，避免 4GB 内存耗尽。
- 数据源代理负责凭证隔离、协议适配、超时/重试、字段采样和 WebSocket 广播；严禁浏览器直连内网数据源。
- 发布服务执行校验、资源清单生成、版本固化和回滚；发布过程幂等，不在请求线程执行大模型转换。
- Docker Compose 管理 nginx、api、postgres、redis 四个容器；不使用 Kubernetes。模型处理可作为按需启动的 `worker` 容器。
- 观测采用应用文件日志 + 基础健康检查；资源允许时再增加 Prometheus/Loki，避免 4GB 机器常驻过多组件。

### 6.4 单机资源与容量约束

| 资源 | 建议约束 |
|---|---|
| CPU | API 进程 2 个 worker；模型处理任务单并发 |
| 内存 | PostgreSQL 约 1GB、Redis 256MB、API 约 1GB，预留系统和突发空间 |
| 磁盘 | 系统盘 70GB；建议追加腾讯云数据盘存放模型，若不追加则限制资源总量并设置配额 |
| 场景规模 | MVP 单场景建议 ≤100 节点，单模型文件 ≤100MB |
| 并发 | 建议同时编辑/运行用户 ≤10，超出后再拆分服务 |
| 上传 | 分片/断点续传暂在 API 内实现，限制单文件和项目总容量 |

## 7. 核心领域模型

```ts
type SceneDocument = {
  schemaVersion: string;
  sceneId: string;
  settings: SceneSettings;
  assets: AssetRef[];
  nodes: SceneNode[];
  dataSources: DataBinding[];
  events: EventRule[];
  timeline?: Timeline;
};

type SceneNode = {
  id: string;
  parentId: string | null;
  type: string;
  name: string;
  transform: { position: Vec3; rotation: Vec3; scale: Vec3 };
  props: Record<string, unknown>;
  visible: boolean;
  locked?: boolean;
  children?: string[];
};

type ComponentDefinition = {
  type: string;
  version: string;
  category: 'basic' | 'data' | 'advanced' | 'system';
  propSchema: JsonSchema;
  dataSchema?: JsonSchema;
  events: EventDefinition[];
  createDefault(): Partial<SceneNode>;
  runtime: string;
};
```

兼容策略：场景文档带 `schemaVersion`；发布时冻结组件版本；升级通过 migration 脚本逐版本迁移，禁止运行时隐式破坏旧版本。

## 8. API 草案

- `POST /api/projects` 创建项目；`GET /api/projects` 分页查询。
- `GET /api/projects/:id/versions/:version` 获取场景文档。
- `PUT /api/projects/:id/draft` 保存草稿（带 `revision` 乐观锁）。
- `POST /api/projects/:id/validate` 执行发布前校验。
- `POST /api/projects/:id/releases` 发布版本；`GET /api/projects/:id/releases` 获取历史版本；`POST /api/releases/:id/rollback` 回滚。
- `GET /api/projects/:id/share-link` 获取访问链接；`POST /api/runtime/errors` 上报基础运行错误。
- `POST /api/assets/upload` 上传资源（V1 支持受限分片/断点续传）；`GET /api/assets/:id` 获取资源元数据和受控访问地址。
- `POST /api/data-sources/test` 测试数据源；`GET /api/data-sources/:id/preview` 获取字段样本。
- `WS /api/runtime/:releaseId` 实时数据和场景事件通道。

安全要求：所有写接口带租户与权限校验；草稿保存使用 revision 防覆盖；数据源凭证仅服务端可解密；WebSocket 按发布版本和项目权限鉴权并限流；本地文件下载必须经过权限校验，禁止直接暴露 `/data` 目录。

## 9. 关键技术方案

### 9.1 拖拽落点与坐标

拖入画布后先通过射线检测与工作平面求交，生成世界坐标；若命中模型表面则按法线设置朝向；无命中时放置在相机前方固定距离。落点经过吸附策略和父节点局部变换换算，最终写入一次 `AddNodeCommand`，确保撤销可逆。

### 9.2 资源加载

资源服务返回 hash、格式、压缩版本和依赖图。运行时按优先级加载：场景骨架 → 首屏可见模型 → 贴图/动画 → 非首屏资源；失败资源显示占位节点并上报错误。所有资源使用内容 hash 命名，Nginx 设置长缓存；未来迁移 COS/CDN 时只替换存储适配器和 URL 策略。

### 9.3 实时数据

浏览器只连接 FastAPI 的运行时 WebSocket 接口；服务端将 REST/WebSocket/MQTT 统一为事件流，按场景订阅分发。客户端采用批量合并、时间窗口和背压策略，渲染帧与数据帧解耦，避免高频数据触发 React 组件树重渲染。V1 不引入独立消息总线。

### 9.4 插件 SDK

插件以 manifest 注册组件、属性控件、运行时 bundle、资源依赖和权限声明。编辑器运行在受控 iframe/Worker 边界；插件不得直接访问 token、DOM 外部页面或任意网络。提供 `registerComponent`、`registerAction`、`registerDataAdapter` 三类扩展点。

## 10. 安全、可靠性与运维

- 租户隔离、RBAC（项目/文件夹/发布环境三级）、SSO/OIDC、操作审计和敏感字段脱敏。
- SSRF 防护：数据源域名白名单、内网地址拒绝、DNS 重绑定检测、出站代理和超时限制。
- 上传安全：文件类型校验、病毒扫描、模型解析沙箱、大小/三角面/纹理尺寸配额。
- XSS/脚本安全：表达式使用 AST 白名单；用户脚本在 Worker 沙箱执行，禁止 `eval` 和任意 import。
- 可观测性：前端错误、资源失败、WebGL 上下文丢失、FPS、发布耗时、数据延迟均打点；日志带 `tenantId/projectId/releaseId/traceId`。
- 备份：使用 `pg_dump` 定时备份 PostgreSQL，并对 `/data/3dvision/assets`、`/data/3dvision/releases` 做增量/压缩备份；备份不要长期只放在本机，应定期下载到本地/NAS。单机方案不具备 PITR 和自动故障转移能力。
- 存储安全：本地资源目录禁止执行权限；上传校验扩展名、MIME、文件大小、GLTF/GLB 结构和模型面数；通过 Nginx 或 API 鉴权后提供文件访问。

## 11. 测试与验收

- 单元测试：schema 校验、命令撤销重做、坐标换算、事件规则、数据转换。
- 集成测试：项目保存/加载、资源上传、发布校验、WebSocket 断线重连、权限矩阵。
- E2E：拖拽组件 → 配置属性 → 绑定数据 → 预览 → 发布 → 分享访问全链路。
- 视觉回归：固定相机、固定资源和浏览器版本下进行截图差异比对。
- 性能测试：50/100/200 节点、1-5 万条实时数据、弱网、磁盘满、进程重启和 WebGL 上下文恢复；不以百万级实例作为 4GB 单机 MVP 指标。
- 安全测试：越权、SSRF、恶意模型、脚本逃逸、资源投毒、令牌重放和发布回滚。

## 12. 研发分期

### Phase 0（1-2 周）

技术验证：Three.js 场景基座、GLTF 加载、dnd-kit 落点、场景 JSON、基础 Gizmo、性能采样。

### Phase 1（4-6 周，MVP）

项目工作台、编辑器布局、基础组件、场景树、属性 schema、撤销重做、静态/REST 数据、预览和版本发布。

### Phase 2（4-6 周）

实时数据、时间轴、素材中心、模板、权限、性能优化、嵌入分享和基础运行错误日志；根据磁盘容量评估是否接入 COS。

### Phase 3（持续演进）

插件市场、GIS/BIM、协同编辑、WebGPU、行业组件包、AI 辅助布局/生成和企业私有化部署。

## 13. MVP 验收清单

- 新建项目后可从左侧拖入立方体、模型、文字、灯光和 3D 图表。
- 选中节点可通过 Gizmo 与属性表单修改，并支持撤销/重做。
- 可绑定 REST JSON，完成字段映射、阈值变色和点击弹窗。
- 刷新浏览器后草稿不丢失；发布生成不可变版本并可回滚。
- 发布后可生成访问链接；无编辑权限的用户可只读运行，资源加载失败和 WebGL 不可用有可理解提示，并能在发布中心查看基础错误日志。
- 典型 100 节点场景桌面 Chrome/Edge FPS ≥30（独立显卡基线），首屏 ≤8 秒；4 核 4GB CVM 上 API、数据库和 Redis 同时运行时不发生 OOM。

## 14. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 大模型/高面数导致卡顿 | 高 | 资产规范、压缩、LOD、实例化、性能预算和发布前检查 |
| schema/组件升级破坏旧项目 | 高 | 组件版本锁定、文档迁移、发布产物冻结 |
| 外部数据源不稳定 | 中 | 服务端代理、缓存、重试、降级数据和断线重连 |
| 浏览器/WebGPU 兼容性 | 中 | WebGL 基线、能力探测、分级渲染和降级提示 |
| 插件执行风险 | 高 | manifest 权限、沙箱、签名、审核和资源扫描 |
| 多人协同复杂度 | 中 | V1 使用乐观锁与版本分支，协同编辑后置 |

## 15. 关键决策建议

1. V1 采用“FastAPI 模块化单体 + Nginx + PostgreSQL + Redis + 本地文件存储”的单机架构，优先控制成本和交付复杂度。
2. 场景文档坚持 JSON Schema 与组件版本化，运行时不依赖编辑器状态。
3. 将性能预算作为产品能力（实时面板、发布校验、资源配额），而不是上线前临时优化。
4. 优先建设组件 SDK、数据适配器和模板体系，这是平台长期复用和生态扩展的核心。
5. 存储抽象为 `StorageBackend` 接口，V1 实现 `LocalStorageBackend`；未来可无痛增加 `TencentCOSBackend`，不修改业务数据模型。

## 16. 单机部署与迁移路径

### 16.1 Docker Compose 服务

```text
nginx      80/443，对外唯一入口，托管前端和资源缓存
api        FastAPI，建议 2 个 Uvicorn worker
postgres   PostgreSQL，端口仅暴露给 Docker 内网
redis      Redis，端口仅暴露给 Docker 内网
worker     按需启动，模型校验/缩略图/压缩任务，单并发
```

PostgreSQL 和 Redis 不开放公网端口；CVM 安全组只放行 SSH（建议限制来源）、80 和 443。应用数据统一挂载 `/data/3dvision`，便于备份和迁移。

### 16.2 超出单机能力后的迁移顺序

1. 先增加独立数据盘，解决系统盘和模型资源争用。
2. 再将模型、贴图和发布产物迁移到腾讯云 COS，启用 CDN。
3. 将 PostgreSQL 迁移到腾讯云托管数据库，Redis 迁移到云 Redis。
4. 将 `worker` 拆到第二台 CVM；最后再考虑 API 多实例和独立 WebSocket 网关。
