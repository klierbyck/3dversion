# 3D 可视化拖拽系统
# 最终需求规格说明书

版本：V1.0  
基于文档：《3D可视化拖拽系统-PRD与技术架构》  
目标读者：产品、UI/UX、前端、后端、测试、部署和实施人员  
部署基线：单台腾讯云 CVM，4 核 CPU、4GB 内存、70GB SSD 系统盘

## 1. 文档目标与范围

本文档是 V1 MVP 的研发执行依据。研发人员应以本文档定义的功能边界、状态流转、接口契约和验收条件为准。未在本文档中定义的能力不属于 V1 必须交付内容，需经过产品变更评审后再实施。

### 1.1 V1 必须交付

- 项目工作台和项目基础信息管理
- 3D 场景编辑器：组件拖拽、场景树、3D 画布、Gizmo、属性配置
- 基础组件、模型组件、文字组件、灯光/相机组件和基础数据图表
- 草稿自动保存、手动保存、刷新恢复和版本冲突提示
- 静态 JSON、REST 数据源和基础字段绑定
- 点击、悬停、数据变化等事件及基础动作
- 预览、发布版本、访问链接、版本回滚
- 基础运行错误日志
- 基础用户、项目权限和资源访问控制
- 单机 Docker Compose 部署和备份脚本

### 1.2 V1 明确不交付

- 多人实时协同编辑
- 多环境、灰度发布、复杂域名管理和完整 APM 平台
- 专业 BIM 编辑、完整 GIS 引擎和复杂物理仿真
- 插件市场和第三方组件审核平台
- WebGPU 专用渲染路径
- 移动端编辑器

## 2. 角色与权限

### 2.1 角色

| 角色 | 说明 |
|---|---|
| 管理员 | 管理用户、项目、组件、资源和系统配置 |
| 设计师 | 创建、编辑、预览、发布和回滚自己有权限的项目 |
| 运营人员 | 修改授权范围内的业务文本、指标和阈值，可预览，不默认拥有发布权 |
| 访客 | 通过访问链接只读运行已发布场景 |

### 2.2 权限矩阵

| 操作 | 管理员 | 设计师 | 运营人员 | 访客 |
|---|---:|---:|---:|---:|
| 查看项目 | 全部 | 授权项目 | 授权项目 | 已发布链接 |
| 创建/删除项目 | 是 | 是 | 否 | 否 |
| 编辑场景 | 是 | 是 | 受限 | 否 |
| 保存草稿 | 是 | 是 | 受限 | 否 |
| 发布/回滚 | 是 | 按项目授权 | 否 | 否 |
| 查看运行错误 | 是 | 项目成员 | 项目成员 | 否 |
| 管理用户/组件 | 是 | 否 | 否 | 否 |

所有接口必须在服务端进行租户、项目和操作级鉴权，不能只依赖前端隐藏按钮。

## 3. 产品页面与布局

### 3.1 工作台

页面包含：项目搜索、项目卡片/表格、创建项目、最近编辑、发布状态、模板入口和回收站入口。

项目卡片字段：名称、缩略图、状态（草稿/已发布）、当前版本、最近编辑人、更新时间。

交互要求：

1. 创建项目必须填写名称，名称长度 1-64 个字符。
2. 删除项目进入回收站，不立即物理删除；管理员可恢复或永久删除。
3. 点击项目进入编辑器；点击访问链接图标进入运行态。
4. 项目列表支持分页，默认每页 20 条。

### 3.2 场景编辑器布局

```text
┌──────────────────────────────────────────────────────┐
│ 顶部工具栏                                           │
├────────────┬──────────────────────────┬──────────────┤
│ 组件/资源区 │        3D 画布           │ 属性配置区   │
│ 320px       │        自适应            │ 360px        │
├────────────┴──────────────────────────┴──────────────┤
│ 时间轴 / 事件区（可折叠，默认高度 240px）              │
└──────────────────────────────────────────────────────┘
```

面板宽度可拖动调整；最小宽度：左侧 260px、右侧 320px、底部 180px。底部区域默认折叠，避免影响 3D 画布。

## 4. 场景编辑器详细需求

### 4.1 顶部工具栏

功能按钮及快捷键：

| 功能 | 快捷键 | 要求 |
|---|---|---|
| 保存 | Ctrl/Cmd+S | 立即提交草稿，显示保存结果 |
| 撤销/重做 | Ctrl/Cmd+Z / Shift+Z | 按命令记录，至少支持 50 步 |
| 复制/粘贴 | Ctrl/Cmd+C/V | 复制节点及必要资源引用，生成新 ID |
| 删除 | Delete/Backspace | 删除前无弹窗，支持撤销恢复 |
| 预览 | Ctrl/Cmd+Enter | 切换只读运行态 |
| 发布 | 无 | 先执行校验，再打开发布确认框 |

工具栏必须显示：项目名称、当前保存状态、当前版本、渲染 FPS、未保存变更数量。

保存状态：`已保存`、`保存中`、`保存失败`、`存在冲突`。保存失败不得清空本地编辑状态。

### 4.2 左侧组件库

#### 4.2.1 分类与搜索

分类：基础、数据、高级、系统、我的收藏。支持按名称、类型和标签搜索；搜索无结果显示空状态和清除按钮。

#### 4.2.2 V1 组件清单

| 组件 | 类型标识 | 必备属性 |
|---|---|---|
| 空节点/组 | `group` | 名称、可见、变换 |
| 立方体 | `mesh.box` | 尺寸、材质、颜色、透明度 |
| 球体 | `mesh.sphere` | 半径、分段、材质 |
| 平面 | `mesh.plane` | 宽高、材质、双面 |
| GLTF/GLB 模型 | `model.gltf` | 资源、缩放、动画、LOD |
| 3D 文字 | `text.3d` | 文本、字体、字号、颜色、朝向 |
| 图片/图标 | `sprite.image` | 图片资源、尺寸、朝向、透明度 |
| 灯光 | `light.point/directional/ambient` | 类型、颜色、强度、阴影 |
| 相机 | `camera.perspective/orthographic` | 位置、视角、近远裁剪面 |
| 柱状图 | `chart.bar3d` | 数据源、分类字段、数值字段、颜色 |
| 折线图 | `chart.line3d` | 数据源、X/Y/Z 字段、线宽、动画 |
| 仪表盘 | `chart.gauge3d` | 数值字段、最小值、最大值、阈值 |
| 标签 | `ui.label` | 文本、背景、跟随节点、偏移 |
| 弹窗 | `ui.popup` | 标题、内容、关联节点、显示条件 |

组件卡片必须显示图标、名称、简短描述和版本。拖拽开始时显示半透明预览；拖拽取消不创建节点。

### 4.3 3D 画布

#### 4.3.1 视图操作

- 左键选择，Ctrl/Cmd 加选，Shift 框选，右键旋转，滚轮缩放，中键平移。
- 支持透视/正交相机切换、视图重置、聚焦选中节点、前/后/左/右/顶/底视图。
- 提供网格、坐标轴、辅助线开关；辅助元素不进入发布产物。
- WebGL 能力检测失败时显示浏览器兼容提示，不允许编辑器静默空白。

#### 4.3.2 拖拽落点规则

1. 优先与可见模型表面射线求交。
2. 未命中模型时与默认工作平面求交。
3. 无有效交点时放置在相机前方 5 个世界单位处。
4. 开启吸附后按网格尺寸吸附，默认网格为 1 单位。
5. 新节点默认加入当前选中组，否则加入根节点。

#### 4.3.3 Gizmo

移动、旋转、缩放三种模式；支持世界/局部坐标切换、数值输入、轴向约束和吸附。Gizmo 操作结束后合并为一条历史命令，连续拖动不产生大量撤销记录。

### 4.4 场景树

场景树展示根节点及所有子节点，支持展开/收起、重命名、分组、拖动改父节点、隐藏、锁定、删除和聚焦。

约束：

- 节点 ID 全局唯一且不可复用。
- 禁止把节点拖入自身或后代节点。
- 删除父节点时弹窗确认：删除子节点或移动子节点到父级。
- 锁定节点后画布不可选中和变换，但属性区可查看。

### 4.5 属性面板

属性面板基于组件定义的 JSON Schema 动态生成。字段修改采用 300ms 防抖写入编辑器状态，失焦或按回车触发校验。

通用属性：名称、可见、锁定、位置、旋转、缩放、父节点、业务 ID、标签。

校验要求：

- 数值字段显示单位和合法范围。
- 颜色字段支持颜色选择器和十六进制输入。
- 资源字段支持选择已有资源和上传新资源。
- 必填字段未完成时，组件状态显示警告，发布校验阻断。
- 修改非法值时保留上一次合法值，并显示错误原因。

### 4.6 数据源与字段绑定

V1 支持：静态 JSON、REST GET、WebSocket。数据源配置字段：名称、地址、请求方法、请求头、参数、刷新周期、超时、认证方式。

流程：创建数据源 → 测试连接 → 获取字段样例 → 选择组件 → 绑定字段 → 配置格式化/阈值 → 预览数据。

安全要求：凭证在服务端加密保存；浏览器只访问 FastAPI 代理；请求超时默认 10 秒，失败最多重试 2 次。

### 4.7 事件与动作

事件规则字段：规则 ID、触发对象、事件类型、条件表达式、动作列表、启用状态。

V1 事件：点击、双击、悬停、数据变化、场景加载完成。  
V1 动作：显示/隐藏、变色、透明度变化、相机聚焦、播放/暂停动画、打开弹窗、刷新数据。

事件条件只允许白名单表达式，不允许 `eval`、任意 import 或访问服务器文件。事件循环超过 10 次必须自动中断并记录错误。

### 4.8 时间轴

V1 支持单场景时间轴和基础关键帧：位置、旋转、缩放、透明度、颜色、可见性。支持播放、暂停、循环、速度（0.25x/0.5x/1x/2x）和当前时间拖动。

时间轴不是 V1 发布阻断项；没有动画的场景不显示错误。关键帧必须可撤销，运行态不显示编辑辅助轨道。

## 5. 草稿、发布和回滚

### 5.1 草稿保存

- 自动保存默认每 10 秒触发，且只在存在变更时执行。
- 手动保存立即执行，接口超时 15 秒后显示失败。
- 服务端以 `revision` 做乐观锁；版本不匹配返回 409，前端保留本地内容并提示用户选择覆盖或重新加载。
- 浏览器异常退出后，重新进入项目优先恢复服务端草稿；存在未提交本地变更时提示恢复。

### 5.2 发布

发布校验包括：JSON Schema、节点父子关系、资源存在性、资源大小/格式、数据源配置、事件表达式、组件版本和磁盘空间。

发布成功后生成：版本号、场景快照、资源清单、发布时间、发布人和访问路径。发布失败不得改变当前线上版本。

### 5.3 访问链接

链接格式建议：`/view/{projectId}`。运行时只加载当前发布版本；项目无发布版本返回 404 业务错误。访问页面不显示编辑器工具栏。

### 5.4 回滚

管理员或有发布权限的设计师选择历史版本并确认后，将项目的 `currentReleaseId` 指向目标版本。回滚操作必须写入审计记录，并提供再次发布新版本的能力。

### 5.5 基础运行错误日志

错误字段：项目 ID、发布版本、错误类型、错误摘要、资源/接口标识、浏览器、时间、traceId。错误类型包括 `asset_load`、`data_request`、`websocket`、`runtime`、`webgl`。

日志页面支持按版本、类型、时间筛选和删除；不采集访问者隐私数据，不记录数据源密码和完整业务响应。

## 6. 后端接口契约

统一响应格式：

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "requestId": "uuid"
}
```

核心接口：

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/projects` | 创建项目 |
| GET | `/api/projects` | 项目分页 |
| GET | `/api/projects/{id}` | 项目详情 |
| PUT | `/api/projects/{id}/draft` | 保存草稿 |
| GET | `/api/projects/{id}/draft` | 获取草稿 |
| POST | `/api/projects/{id}/validate` | 发布前校验 |
| POST | `/api/projects/{id}/releases` | 创建发布版本 |
| GET | `/api/projects/{id}/releases` | 历史版本 |
| POST | `/api/releases/{id}/rollback` | 回滚版本 |
| GET | `/api/projects/{id}/share-link` | 获取访问链接 |
| GET | `/api/runtime/{projectId}` | 运行态场景数据 |
| POST | `/api/runtime/errors` | 上报运行错误 |
| POST | `/api/assets/upload` | 上传资源 |
| GET | `/api/assets/{id}` | 资源元数据 |
| POST | `/api/data-sources/test` | 测试数据源 |
| WS | `/api/runtime/{projectId}/ws` | 实时数据通道 |

## 7. 核心数据表

- `users`：用户、角色、状态、密码哈希、创建时间。
- `projects`：项目名称、所有者、状态、当前发布 ID、回收站状态。
- `project_members`：项目成员、角色和权限范围。
- `scene_drafts`：项目 ID、revision、场景 JSONB、更新时间、编辑人。
- `releases`：项目 ID、版本号、场景快照、资源清单、发布人、状态、发布时间。
- `assets`：文件名、类型、大小、hash、本地存储路径、状态、上传人。
- `data_sources`：数据源配置密文、类型、超时和状态。
- `runtime_errors`：发布版本、错误类型、摘要、资源/接口标识、浏览器和时间。
- `audit_logs`：用户、项目、动作、目标、结果和时间。

场景 JSONB 由 `schemaVersion` 标识；发布时保存完整快照，不依赖草稿实时变化。

## 8. 单机部署要求

服务器：腾讯云 CVM，4 核 CPU、4GB 内存、70GB SSD 系统盘。应用通过 Docker Compose 部署：

```text
nginx      80/443，对外唯一入口
api        FastAPI，建议 2 个 Uvicorn worker
postgres   仅 Docker 内网访问
redis      仅 Docker 内网访问，最大内存 256MB
worker     按需启动，模型任务单并发
```

资源目录：

```text
/data/3dvision/
├── assets/
├── releases/
├── uploads-tmp/
└── backups/
```

安全组只开放 SSH、80、443；PostgreSQL 5432 和 Redis 6379 不开放公网。模型文件和发布产物不得放入数据库 BLOB；数据库仅保存路径、hash 和元数据。

## 9. 测试与验收标准

### 9.1 功能验收

1. 用户可创建项目并进入编辑器。
2. 可拖入 V1 组件，修改属性并通过撤销/重做恢复。
3. 场景树可重命名、隐藏、锁定、分组和改变父子关系。
4. 刷新页面后草稿完整恢复；并发保存冲突有明确提示。
5. REST JSON 可绑定图表/标签，数据请求失败有错误状态。
6. 点击节点可执行变色、相机聚焦、弹窗和显隐动作。
7. 可发布版本并生成访问链接；访问态不显示编辑器控件。
8. 可从历史版本回滚，回滚后访问链接展示目标版本。
9. 运行错误可在发布中心按版本和类型查询。

### 9.2 性能与稳定性验收

- 100 节点典型场景在桌面 Chrome/Edge 独立显卡基线下 FPS ≥30。
- 发布场景首屏加载 ≤8 秒（资源总量不超过 100MB）。
- 4 核 4GB 服务器同时运行 API、PostgreSQL、Redis 时无 OOM。
- 10 个并发运行访问、2 个编辑会话下，保存接口 P95 ≤300ms。
- API、PostgreSQL、Redis 重启后数据不丢失；草稿和已发布版本可恢复。

### 9.3 安全验收

- 未授权用户不能读取草稿、未发布资源和数据源凭证。
- PostgreSQL/Redis 不可从公网连接。
- 上传脚本文件、超大文件和非法模型被拒绝。
- 事件表达式不能执行任意代码。
- 错误日志不包含密码、Token 和完整业务数据。

## 10. 研发拆分

### 前端任务

- 编辑器壳层、面板布局和快捷键
- Three.js 场景运行时、选择器、Gizmo、射线落点
- 组件注册协议和 V1 组件实现
- 场景树、属性 Schema 表单和命令历史
- 数据绑定、事件编排、基础时间轴
- 草稿保存状态、预览、发布确认、版本列表和运行错误展示

### 后端任务

- FastAPI 工程骨架、鉴权、统一响应和异常处理
- 项目、草稿、版本、回滚和审计接口
- JSON Schema 校验和发布前检查
- 本地文件存储、上传校验、资源访问鉴权
- REST/WebSocket 数据代理
- 运行错误日志接口和查询接口
- PostgreSQL 迁移、Redis 缓存和单机备份脚本

### 测试任务

- 组件属性和场景 JSON 单测
- 保存冲突、发布失败、回滚和资源访问权限测试
- 浏览器 E2E 全链路测试
- 4 核 4GB 单机压力、磁盘满和进程重启测试
- WebGL 不可用、模型加载失败和 WebSocket 断线测试

## 11. 交付定义

只有同时满足以下条件，V1 才视为完成：

- 功能验收清单全部通过。
- API 文档、数据库迁移脚本和 Docker Compose 文件齐全。
- 关键页面和接口有自动化测试。
- 有 PostgreSQL 和本地资源备份恢复演练记录。
- 生产部署不暴露数据库、Redis 和本地根目录。
- 未实现功能、已知限制和后续迁移方案已记录。
