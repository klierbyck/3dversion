# TODO

## 当前功能已完成

- [x] 真实资源上传：GLB、单文件 GLTF、PNG、JPG、WebP，包含格式签名与结构校验。
- [x] GLTF/GLB 通过 Three.js `GLTFLoader` 加载，支持内嵌动画自动播放、暂停和片段切换。
- [x] 图片资源显示真实缩略图，可点击加入画布；资源不再支持拖拽加入，避免误操作。
- [x] 资源删除、名称省略、列表无横向滚动，资源区与场景树各占剩余高度的一半。
- [x] 项目、草稿、发布记录、资源索引和运行错误持久化到 `DATA_DIR`，服务重启后可恢复。
- [x] 数据源配置：静态 JSON、REST GET/POST、WebSocket，支持参数、请求头、请求体和刷新周期。
- [x] 数据源凭证独立加密保存，草稿、发布产物和运行态接口不返回明文。
- [x] 数据源代理限制私网/本机访问，逐跳校验重定向并限制响应体大小。
- [x] 数据字段可绑定 `value`、`text`、`color`、`opacity`、`visible`，支持格式化与颜色阈值。
- [x] 柱图、折线图、仪表盘随数据绑定值实时更新，文字和通用组件同步响应绑定。
- [x] 六个内置 Demo 包含多种数据源与绑定效果。
- [x] 场景时间轴支持秒、分、时切换，支持速度、循环、对象关键帧和相机关键帧。
- [x] 撤销/重做、多选、场景树、属性面板、事件动作和发布前校验均已接入。
- [x] 前端单元测试、后端接口测试、生产构建和桌面浏览器布局回归已通过。
- [x] Schema 专项测试覆盖组件字段、嵌套更新、数值边界、序列解析、节点关系校验和发布阻断。
- [x] 字体运行时资源与生成工具分离；TTF 和字符表中间产物已加入 `.gitignore`。
- [x] README 已按真实资源、磁盘持久化、凭证安全、数据源和验证方式更新。

## 后续生产化增强

- [ ] 用户登录、RBAC、项目成员权限与资源访问鉴权。
- [ ] PostgreSQL 多实例数据仓储和 Redis 分布式任务队列。
- [ ] 对象存储/CDN、超大模型分片上传、压缩和 LOD 生成。
- [ ] 独立公开访问页 `#/view/:projectId`、分享链接权限和访问过期策略。
- [ ] 完整审计日志、监控告警、备份恢复演练和容量治理。
- [ ] 按路由拆包，降低当前 Three.js 主包体积警告。
